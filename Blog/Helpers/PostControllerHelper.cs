using Blog.ViewModels;

namespace Blog.Helpers
{
    public static class PostControllerHelper
    {
        //    public static List<HeaderPostViewModel> GetHeaderPostViewModelsList(string username) {

        //    var posts = _repo.GetAllUserPosts(User.Identity.Name);

        //    List<HeaderPostViewModel> list = new();

        //        foreach (Post post in posts)
        //        {
        //            list.Add(new HeaderPostViewModel
        //            {
        //                Id = post.Id,
        //                Author = post.Author,
        //                Title = post.Title
        //            });
        //        }

        //}
    }
}
